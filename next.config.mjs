//next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['th.bing.com', 'c10.patreonusercontent.com'], // Add the required external domains for images
  },
};

export default nextConfig;

