export const metadata = {
    title: "Signup - Join Subhan Portfolio",
    description: "Create an account to visit Projects in Suban Portfolio.",
  };
  
  export default function SignupLayout({ children }) {
    return (
      <div>
        {children}
      </div>
    );
  }
  