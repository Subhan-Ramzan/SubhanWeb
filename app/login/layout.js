export const metadata = {
    title: "Login - Subhan Portfolio",
    description: "Log in to access your profile and visit Projects in Subhan Portfolio.",
  };
  
  export default function LoginLayout({ children }) {
    return (
      <div>
        {children}
      </div>
    );
  }
  